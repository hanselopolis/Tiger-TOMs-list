<style>
    h1 {text-align: center;}
</style>

<h1>Category: <%= @category.title%></h1>

<div class= "picture-grid">
    <% @category.ads.each do |ad| %>
            <% if ad.images.attached? %>
                <div class= "grid-box">
                    <a href= "<%= ad_url(@category, ad)%>">
                        <p class= "ad-button"><%= image_tag(ad.images[0])%> <%= ad.title %></p>
                    </a>
                </div>
            <% end %>
        
</div>

<% end %>